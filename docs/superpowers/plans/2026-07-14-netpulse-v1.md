# NetPulse v1 Implementation Plan (Revised)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a real-time network monitoring dashboard with WebSocket-powered live metrics, dark theme, and notification system.

**Architecture:** React (TypeScript) frontend with react-router-dom communicates with FastAPI backend via single WebSocket (`/ws/live`) for all real-time data and REST API for manual actions. Backend uses psutil/ping3 for system metrics, handles alert detection, and pushes notifications to frontend.

**Tech Stack:** React, TypeScript, Vite, Tailwind CSS, shadcn/ui, Recharts, Lucide Icons, react-router-dom, FastAPI, WebSocket, psutil, ping3, speedtest-cli, pnpm, Vitest, React Testing Library

## Global Constraints

- Python 3.11+
- Node.js 18+
- TypeScript for all frontend code
- pnpm as package manager
- Single WebSocket connection for all real-time data
- Backend handles notification logic
- Speedtest runs as background job
- .env for all configuration
- ruff + black for backend formatting
- eslint + prettier for frontend formatting
- Vitest + React Testing Library for frontend tests

---

## File Structure

```
NetPulse/
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── ui/              # shadcn/ui components
│   │   │   ├── layout/
│   │   │   │   ├── Sidebar.tsx
│   │   │   │   └── Header.tsx
│   │   │   ├── cards/
│   │   │   │   ├── CpuCard.tsx
│   │   │   │   ├── RamCard.tsx
│   │   │   │   ├── DiskCard.tsx
│   │   │   │   ├── NetworkCard.tsx
│   │   │   │   ├── PingCard.tsx
│   │   │   │   └── SystemInfoCard.tsx
│   │   │   ├── charts/
│   │   │   │   ├── CpuChart.tsx
│   │   │   │   ├── TrafficChart.tsx
│   │   │   │   └── PingChart.tsx
│   │   │   └── notifications/
│   │   │       └── Toast.tsx
│   │   ├── pages/
│   │   │   ├── DashboardPage.tsx
│   │   │   ├── SettingsPage.tsx
│   │   │   └── TraceroutePage.tsx
│   │   ├── hooks/
│   │   │   ├── useWebSocket.ts
│   │   │   ├── useSettings.ts
│   │   │   └── useChartData.ts
│   │   ├── services/
│   │   │   └── api.ts
│   │   ├── types/
│   │   │   └── index.ts
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   └── index.css
│   ├── .env
│   ├── .env.example
│   ├── package.json
│   ├── tsconfig.json
│   ├── vite.config.ts
│   ├── tailwind.config.ts
│   ├── postcss.config.js
│   ├── vitest.config.ts
│   └── prettier.config.js
│
├── backend/
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py
│   │   ├── config.py            # Settings from .env
│   │   ├── logging.py           # Logging configuration
│   │   ├── routers/
│   │   │   ├── __init__.py
│   │   │   ├── system.py
│   │   │   ├── network.py
│   │   │   ├── ping.py
│   │   │   ├── speedtest.py
│   │   │   ├── traceroute.py
│   │   │   ├── settings.py
│   │   │   └── websocket.py
│   │   ├── services/
│   │   │   ├── __init__.py
│   │   │   ├── system_info.py
│   │   │   ├── network_info.py
│   │   │   ├── ping_service.py
│   │   │   ├── speed_service.py
│   │   │   └── notification_service.py
│   │   ├── models/
│   │   │   ├── __init__.py
│   │   │   └── schemas.py
│   │   └── jobs/
│   │       ├── __init__.py
│   │       └── background.py    # Background job runner
│   ├── .env
│   ├── .env.example
│   ├── requirements.txt
│   ├── pyproject.toml           # ruff + black config
│   └── tests/
│       ├── __init__.py
│       ├── conftest.py
│       ├── test_system.py
│       ├── test_network.py
│       └── test_ping.py
│
├── data/                        # Settings JSON files
├── docs/
├── README.md
├── .gitignore
└── docker-compose.yml
```

---

## Task 1: Project Scaffolding

**Files:**
- Create: `frontend/package.json` (pnpm)
- Create: `frontend/tsconfig.json`
- Create: `frontend/vite.config.ts`
- Create: `frontend/tailwind.config.ts`
- Create: `frontend/postcss.config.js`
- Create: `frontend/src/main.tsx`
- Create: `frontend/src/App.tsx`
- Create: `frontend/src/index.css`
- Create: `frontend/.env`
- Create: `frontend/.env.example`
- Create: `backend/pyproject.toml`
- Create: `backend/requirements.txt`
- Create: `backend/app/__init__.py`
- Create: `backend/app/main.py`
- Create: `backend/app/config.py`
- Create: `backend/.env`
- Create: `backend/.env.example`
- Create: `.gitignore`

**Interfaces:**
- Produces: Running React dev server on port from .env
- Produces: Running FastAPI server on port from .env

- [ ] **Step 1: Initialize React frontend with pnpm**

```bash
cd frontend
pnpm create vite . --template react-ts
pnpm install
```

- [ ] **Step 2: Install frontend dependencies**

```bash
pnpm add tailwindcss @tailwindcss/vite
pnpm add recharts lucide-react react-router-dom
pnpm add -D @types/node vitest @testing-library/react @testing-library/jest-dom jsdom prettier eslint
```

- [ ] **Step 3: Create .env files**

```bash
# frontend/.env
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000

# frontend/.env.example
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000
```

```bash
# backend/.env
API_PORT=8000
FRONTEND_URL=http://localhost:5173
DEFAULT_PING_HOST=google.com
REFRESH_RATE=1
LOG_LEVEL=INFO

# backend/.env.example
API_PORT=8000
FRONTEND_URL=http://localhost:5173
DEFAULT_PING_HOST=google.com
REFRESH_RATE=1
LOG_LEVEL=INFO
```

- [ ] **Step 4: Configure Vite**

```typescript
// frontend/vite.config.ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'
import path from 'path'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 5173,
    proxy: {
      '/api': 'http://localhost:8000',
      '/ws': {
        target: 'ws://localhost:8000',
        ws: true,
      },
    },
  },
})
```

- [ ] **Step 5: Configure TypeScript**

```json
// frontend/tsconfig.json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

- [ ] **Step 6: Configure Tailwind**

```css
/* frontend/src/index.css */
@import "tailwindcss";
```

- [ ] **Step 7: Create basic App.tsx**

```typescript
// frontend/src/App.tsx
function App() {
  return (
    <div className="min-h-screen bg-slate-950 text-white">
      <h1 className="text-2xl p-4">NetPulse</h1>
    </div>
  )
}
export default App
```

- [ ] **Step 8: Setup Python backend**

```bash
cd backend
python -m venv venv
venv\Scripts\activate
pip install fastapi uvicorn websockets psutil ping3 requests python-dotenv
pip freeze > requirements.txt
```

- [ ] **Step 9: Create backend config**

```python
# backend/app/config.py
from pydantic_settings import BaseSettings
from dotenv import load_dotenv

load_dotenv()

class Settings(BaseSettings):
    api_port: int = 8000
    frontend_url: str = "http://localhost:5173"
    default_ping_host: str = "google.com"
    refresh_rate: int = 1
    log_level: str = "INFO"

settings = Settings()
```

- [ ] **Step 10: Create FastAPI main.py**

```python
# backend/app/main.py
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from .config import settings
import logging

logging.basicConfig(level=settings.log_level)
logger = logging.getLogger(__name__)

app = FastAPI(title="NetPulse API")

app.add_middleware(
    CORSMiddleware,
    allow_origins=[settings.frontend_url],
    allow_methods=["*"],
    allow_headers=["*"],
)

logger.info("NetPulse API starting on port %d", settings.api_port)

@app.get("/api/health")
async def health():
    return {"status": "ok"}
```

- [ ] **Step 11: Configure pyproject.toml**

```toml
# backend/pyproject.toml
[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.black]
line-length = 100
target-version = ["py311"]
```

- [ ] **Step 12: Run both servers and verify**

```bash
# Terminal 1
cd frontend && pnpm dev

# Terminal 2
cd backend && uvicorn app.main:app --reload --port 8000
```

Visit http://localhost:5173 — should see "NetPulse" heading.

- [ ] **Step 13: Commit**

```bash
git add -A
git commit -m "feat: project scaffolding - React TS + FastAPI + pnpm + .env"
```

---

## Task 2: Backend - System Info Service

**Files:**
- Create: `backend/app/services/system_info.py`
- Create: `backend/app/routers/system.py`
- Create: `backend/app/models/schemas.py`
- Modify: `backend/app/main.py`

**Interfaces:**
- Produces: `GET /api/system` → CPU, RAM, Disk, Hostname, OS

- [ ] **Step 1: Create Pydantic schemas**

```python
# backend/app/models/schemas.py
from pydantic import BaseModel
from datetime import datetime

class SystemInfo(BaseModel):
    hostname: str
    os: str
    cpu_model: str
    cpu_cores: int
    cpu_percent: float
    ram_total_gb: float
    ram_used_gb: float
    ram_percent: float
    disk_total_gb: float
    disk_used_gb: float
    disk_percent: float
    python_version: str

class NetworkInfo(BaseModel):
    ipv4: str
    ipv6: str
    mac_address: str
    gateway: str
    dns: str
    public_ip: str
    interface_name: str
    interface_status: str

class TrafficData(BaseModel):
    upload_mbps: float
    download_mbps: float
    packets_sent: int
    packets_recv: int

class PingData(BaseModel):
    host: str
    latency_ms: float | None
    status: str

class LiveUpdate(BaseModel):
    type: str
    data: dict
    timestamp: float
```

- [ ] **Step 2: Create system_info service**

```python
# backend/app/services/system_info.py
import psutil
import platform
import socket
import logging

logger = logging.getLogger(__name__)

def get_system_info() -> dict:
    logger.debug("Fetching system info")
    cpu_percent = psutil.cpu_percent(interval=0.1)
    ram = psutil.virtual_memory()
    disk = psutil.disk_usage('/')

    return {
        "hostname": socket.gethostname(),
        "os": f"{platform.system()} {platform.release()}",
        "cpu_model": platform.processor() or "Unknown",
        "cpu_cores": psutil.cpu_count(),
        "cpu_percent": cpu_percent,
        "ram_total_gb": round(ram.total / (1024**3), 2),
        "ram_used_gb": round(ram.used / (1024**3), 2),
        "ram_percent": ram.percent,
        "disk_total_gb": round(disk.total / (1024**3), 2),
        "disk_used_gb": round(disk.used / (1024**3), 2),
        "disk_percent": disk.percent,
        "python_version": platform.python_version(),
    }
```

- [ ] **Step 3: Create system router**

```python
# backend/app/routers/system.py
from fastapi import APIRouter
from ..services.system_info import get_system_info

router = APIRouter(prefix="/api", tags=["system"])

@router.get("/system")
async def system():
    return get_system_info()
```

- [ ] **Step 4: Register router in main.py**

```python
# backend/app/main.py
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from .config import settings
from .routers import system
import logging

logging.basicConfig(level=settings.log_level)
logger = logging.getLogger(__name__)

app = FastAPI(title="NetPulse API")

app.add_middleware(
    CORSMiddleware,
    allow_origins=[settings.frontend_url],
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(system.router)

@app.get("/api/health")
async def health():
    return {"status": "ok"}
```

- [ ] **Step 5: Test endpoint**

```bash
curl http://localhost:8000/api/system
```

Should return JSON with hostname, cpu_percent, ram_percent, etc.

- [ ] **Step 6: Commit**

```bash
git add backend/
git commit -m "feat: system info service with logging"
```

---

## Task 3: Backend - Network Info Service

**Files:**
- Create: `backend/app/services/network_info.py`
- Create: `backend/app/routers/network.py`
- Modify: `backend/app/main.py`

**Interfaces:**
- Produces: `GET /api/network` → IPv4, IPv6, MAC, Gateway, DNS, Public IP

- [ ] **Step 1: Create network_info service**

```python
# backend/app/services/network_info.py
import psutil
import socket
import requests
import subprocess
import platform
import logging

logger = logging.getLogger(__name__)

def get_network_info() -> dict:
    logger.debug("Fetching network info")
    stats = psutil.net_if_stats()
    addrs = psutil.net_if_addrs()

    interface_name = "Unknown"
    interface_status = "Down"
    ipv4 = "Unknown"
    mac_address = "Unknown"

    for name, stat in stats.items():
        if stat.isup and name != "lo":
            interface_name = name
            interface_status = "Up"
            if name in addrs:
                for addr in addrs[name]:
                    if addr.family == socket.AF_INET:
                        ipv4 = addr.address
                    elif addr.family == psutil.AF_LINK:
                        mac_address = addr.address
            break

    try:
        result = subprocess.run(["ipconfig"], capture_output=True, text=True)
        gateway = "Unknown"
        for line in result.stdout.split("\n"):
            if "Default Gateway" in line and ":" in line:
                gateway = line.split(":")[-1].strip()
                if gateway:
                    break
    except Exception:
        gateway = "Unknown"

    try:
        dns = socket.gethostbyname("google.com")
    except Exception:
        dns = "Unknown"

    try:
        public_ip = requests.get("https://api.ipify.org", timeout=5).text
    except Exception:
        public_ip = "Unknown"

    return {
        "ipv4": ipv4,
        "ipv6": "N/A",
        "mac_address": mac_address,
        "gateway": gateway,
        "dns": dns,
        "public_ip": public_ip,
        "interface_name": interface_name,
        "interface_status": interface_status,
    }
```

- [ ] **Step 2: Create network router**

```python
# backend/app/routers/network.py
from fastapi import APIRouter
from ..services.network_info import get_network_info

router = APIRouter(prefix="/api", tags=["network"])

@router.get("/network")
async def network():
    return get_network_info()
```

- [ ] **Step 3: Register router**

```python
# Add to main.py
from .routers import system, network

app.include_router(system.router)
app.include_router(network.router)
```

- [ ] **Step 4: Test endpoint**

```bash
curl http://localhost:8000/api/network
```

- [ ] **Step 5: Commit**

```bash
git add backend/
git commit -m "feat: network info service"
```

---

## Task 4: Backend - Unified WebSocket

**Files:**
- Create: `backend/app/routers/websocket.py`
- Create: `backend/app/services/notification_service.py`
- Modify: `backend/app/main.py`

**Interfaces:**
- Produces: `/ws/live` → pushes all real-time data every 1s

- [ ] **Step 1: Create notification service (backend detects alerts)**

```python
# backend/app/services/notification_service.py
import logging

logger = logging.getLogger(__name__)

ALERTS = []

def check_alerts(system_data: dict, ping_data: dict) -> list[dict]:
    new_alerts = []

    # CPU alert
    if system_data.get("cpu_percent", 0) > 90:
        alert = {
            "type": "cpu_high",
            "message": f"High CPU: {system_data['cpu_percent']}%",
            "severity": "warning",
        }
        new_alerts.append(alert)
        logger.warning("CPU alert triggered: %s%%", system_data["cpu_percent"])

    # RAM alert
    if system_data.get("ram_percent", 0) > 85:
        alert = {
            "type": "ram_high",
            "message": f"High RAM: {system_data['ram_percent']}%",
            "severity": "warning",
        }
        new_alerts.append(alert)
        logger.warning("RAM alert triggered: %s%%", system_data["ram_percent"])

    # Ping alert
    if ping_data and ping_data.get("status") == "timeout":
        alert = {
            "type": "ping_timeout",
            "message": f"Ping timeout to {ping_data.get('host', 'unknown')}",
            "severity": "error",
        }
        new_alerts.append(alert)
        logger.error("Ping timeout: %s", ping_data.get("host"))

    return new_alerts
```

- [ ] **Step 2: Create unified WebSocket endpoint**

```python
# backend/app/routers/websocket.py
from fastapi import APIRouter, WebSocket, WebSocketDisconnect, Query
import asyncio
import psutil
import time
import logging
from ..services.ping_service import ping_host
from ..services.notification_service import check_alerts
from ..config import settings

logger = logging.getLogger(__name__)
router = APIRouter()

@router.websocket("/ws/live")
async def ws_live(websocket: WebSocket, host: str = Query(default=None)):
    await websocket.accept()
    ping_target = host or settings.default_ping_host
    logger.info("WebSocket connected, ping target: %s", ping_target)

    prev_traffic = psutil.net_io_counters()
    alert_cooldown = {}

    try:
        while True:
            # System data
            system_data = {
                "cpu_percent": psutil.cpu_percent(interval=0.1),
                "ram_percent": psutil.virtual_memory().percent,
                "disk_percent": psutil.disk_usage('/').percent,
            }

            # Traffic data
            curr_traffic = psutil.net_io_counters()
            traffic_data = {
                "upload_mbps": round(
                    (curr_traffic.bytes_sent - prev_traffic.bytes_sent) / 1024 / 1024, 2
                ),
                "download_mbps": round(
                    (curr_traffic.bytes_recv - prev_traffic.bytes_recv) / 1024 / 1024, 2
                ),
                "packets_sent": curr_traffic.packets_sent,
                "packets_recv": curr_traffic.packets_recv,
            }
            prev_traffic = curr_traffic

            # Ping data
            ping_data = ping_host(ping_target)

            # Check alerts (with cooldown)
            now = time.time()
            alerts = check_alerts(system_data, ping_data)
            filtered_alerts = []
            for alert in alerts:
                last_time = alert_cooldown.get(alert["type"], 0)
                if now - last_time > 5:  # 5s cooldown
                    filtered_alerts.append(alert)
                    alert_cooldown[alert["type"]] = now

            # Send unified update
            await websocket.send_json({
                "type": "live",
                "data": {
                    "system": system_data,
                    "traffic": traffic_data,
                    "ping": ping_data,
                    "alerts": filtered_alerts,
                },
                "timestamp": now,
            })

            await asyncio.sleep(settings.refresh_rate)
    except WebSocketDisconnect:
        logger.info("WebSocket disconnected")
```

- [ ] **Step 3: Register WebSocket router**

```python
# backend/app/main.py
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from .config import settings
from .routers import system, network, websocket
import logging

logging.basicConfig(level=settings.log_level)
logger = logging.getLogger(__name__)

app = FastAPI(title="NetPulse API")

app.add_middleware(
    CORSMiddleware,
    allow_origins=[settings.frontend_url],
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(system.router)
app.include_router(network.router)
app.include_router(websocket.router)

@app.get("/api/health")
async def health():
    return {"status": "ok"}
```

- [ ] **Step 4: Test WebSocket**

```bash
pip install websocket-client
python -c "
import websocket
import json
ws = websocket.create_connection('ws://localhost:8000/ws/live')
for i in range(3):
    data = json.loads(ws.recv())
    print(f'CPU: {data[\"data\"][\"system\"][\"cpu_percent\"]}%')
ws.close()
"
```

- [ ] **Step 5: Commit**

```bash
git add backend/
git commit -m "feat: unified WebSocket + backend notification detection"
```

---

## Task 5: Backend - Ping Service

**Files:**
- Create: `backend/app/services/ping_service.py`
- Create: `backend/app/routers/ping.py`
- Modify: `backend/app/main.py`

**Interfaces:**
- Produces: `GET /api/ping?host=google.com` → single ping result

- [ ] **Step 1: Create ping service**

```python
# backend/app/services/ping_service.py
from ping3 import ping
import logging

logger = logging.getLogger(__name__)

def ping_host(host: str) -> dict:
    logger.debug("Pinging %s", host)
    try:
        latency = ping(host, timeout=5)
        if latency is not None:
            return {
                "host": host,
                "latency_ms": round(latency * 1000, 2),
                "status": "online",
            }
        else:
            return {
                "host": host,
                "latency_ms": None,
                "status": "timeout",
            }
    except Exception as e:
        logger.error("Ping error for %s: %s", host, e)
        return {
            "host": host,
            "latency_ms": None,
            "status": "error",
            "error": str(e),
        }
```

- [ ] **Step 2: Create ping router**

```python
# backend/app/routers/ping.py
from fastapi import APIRouter, Query
from ..services.ping_service import ping_host

router = APIRouter(prefix="/api", tags=["ping"])

@router.get("/ping")
async def ping(host: str = Query(default="google.com")):
    return ping_host(host)
```

- [ ] **Step 3: Register router**

```python
# Add to main.py
from .routers import system, network, websocket, ping

app.include_router(ping.router)
```

- [ ] **Step 4: Test endpoint**

```bash
curl http://localhost:8000/api/ping?host=google.com
```

- [ ] **Step 5: Commit**

```bash
git add backend/
git commit -m "feat: ping service"
```

---

## Task 6: Backend - Speedtest (Background Job)

**Files:**
- Create: `backend/app/jobs/background.py`
- Create: `backend/app/services/speed_service.py`
- Create: `backend/app/routers/speedtest.py`
- Modify: `backend/app/main.py`

**Interfaces:**
- Produces: `POST /api/speedtest` → `{"job_id": "...", "status": "running"}`
- Produces: `GET /api/speedtest/{job_id}` → job status + result

- [ ] **Step 1: Create background job runner**

```python
# backend/app/jobs/background.py
import asyncio
import uuid
import logging
from typing import Any

logger = logging.getLogger(__name__)

jobs: dict[str, dict] = {}

async def run_speedtest_job(job_id: str):
    logger.info("Starting speedtest job: %s", job_id)
    jobs[job_id] = {"status": "running", "result": None}

    try:
        import speedtest
        st = speedtest.Speedtest()
        st.get_best_server()

        download = st.download() / 1_000_000
        upload = st.upload() / 1_000_000
        latency = st.results.ping

        jobs[job_id] = {
            "status": "done",
            "result": {
                "download_mbps": round(download, 2),
                "upload_mbps": round(upload, 2),
                "latency_ms": round(latency, 2),
                "server": str(st.best_host),
            },
        }
        logger.info("Speedtest job %s completed", job_id)
    except Exception as e:
        jobs[job_id] = {"status": "error", "error": str(e)}
        logger.error("Speedtest job %s failed: %s", job_id, e)

def create_speedtest_job() -> str:
    job_id = str(uuid.uuid4())
    asyncio.create_task(run_speedtest_job(job_id))
    return job_id

def get_job_status(job_id: str) -> dict | None:
    return jobs.get(job_id)
```

- [ ] **Step 2: Create speedtest router**

```python
# backend/app/routers/speedtest.py
from fastapi import APIRouter
from ..jobs.background import create_speedtest_job, get_job_status

router = APIRouter(prefix="/api", tags=["speedtest"])

@router.post("/speedtest")
async def start_speedtest():
    job_id = create_speedtest_job()
    return {"job_id": job_id, "status": "running"}

@router.get("/speedtest/{job_id}")
async def speedtest_status(job_id: str):
    job = get_job_status(job_id)
    if job is None:
        return {"status": "not_found"}
    return job
```

- [ ] **Step 3: Register router**

```python
# Add to main.py
from .routers import system, network, websocket, ping, speedtest

app.include_router(speedtest.router)
```

- [ ] **Step 4: Test endpoints**

```bash
# Start job
curl -X POST http://localhost:8000/api/speedtest
# Returns: {"job_id": "xxx", "status": "running"}

# Check status
curl http://localhost:8000/api/speedtest/{job_id}
# Returns: {"status": "done", "result": {...}}
```

- [ ] **Step 5: Commit**

```bash
git add backend/
git commit -m "feat: speedtest as background job"
```

---

## Task 7: Backend - Settings (JSON, not SQLite)

**Files:**
- Create: `backend/app/routers/settings.py`
- Modify: `backend/app/main.py`

**Interfaces:**
- Produces: `GET /api/settings`, `PUT /api/settings`

- [ ] **Step 1: Create settings router (JSON file)**

```python
# backend/app/routers/settings.py
from fastapi import APIRouter
from pydantic import BaseModel
import json
import os
from ..config import settings

router = APIRouter(prefix="/api", tags=["settings"])

SETTINGS_FILE = os.path.join(os.path.dirname(__file__), "../../../data/settings.json")

DEFAULT_SETTINGS = {
    "refresh_rate": settings.refresh_rate,
    "theme": "dark",
    "ping_target": settings.default_ping_host,
    "notifications_enabled": True,
}

def load_settings() -> dict:
    if os.path.exists(SETTINGS_FILE):
        with open(SETTINGS_FILE, "r") as f:
            return json.load(f)
    return DEFAULT_SETTINGS.copy()

def save_settings(data: dict):
    os.makedirs(os.path.dirname(SETTINGS_FILE), exist_ok=True)
    with open(SETTINGS_FILE, "w") as f:
        json.dump(data, f, indent=2)

class SettingsUpdate(BaseModel):
    refresh_rate: int | None = None
    theme: str | None = None
    ping_target: str | None = None
    notifications_enabled: bool | None = None

@router.get("/settings")
async def get_settings():
    return load_settings()

@router.put("/settings")
async def update_settings(update: SettingsUpdate):
    current = load_settings()
    if update.refresh_rate is not None:
        current["refresh_rate"] = update.refresh_rate
    if update.theme is not None:
        current["theme"] = update.theme
    if update.ping_target is not None:
        current["ping_target"] = update.ping_target
    if update.notifications_enabled is not None:
        current["notifications_enabled"] = update.notifications_enabled
    save_settings(current)
    return current
```

- [ ] **Step 2: Register router**

```python
# Add to main.py
from .routers import system, network, websocket, ping, speedtest, settings

app.include_router(settings.router)
```

- [ ] **Step 3: Test endpoints**

```bash
curl http://localhost:8000/api/settings
curl -X PUT http://localhost:8000/api/settings -H "Content-Type: application/json" -d '{"theme": "light"}'
```

- [ ] **Step 4: Commit**

```bash
git add backend/
git commit -m "feat: settings with JSON storage"
```

---

## Task 8: Backend - Traceroute

**Files:**
- Create: `backend/app/routers/traceroute.py`
- Modify: `backend/app/main.py`

**Interfaces:**
- Produces: `POST /api/traceroute` → list of hops

- [ ] **Step 1: Create traceroute router**

```python
# backend/app/routers/traceroute.py
from fastapi import APIRouter
from pydantic import BaseModel
import subprocess
import platform
import logging

logger = logging.getLogger(__name__)
router = APIRouter(prefix="/api", tags=["traceroute"])

class TracerouteRequest(BaseModel):
    host: str

@router.post("/traceroute")
async def traceroute(req: TracerouteRequest):
    logger.info("Traceroute to %s", req.host)
    try:
        cmd = "tracert" if platform.system() == "Windows" else "traceroute"
        result = subprocess.run(
            [cmd, "-d", req.host],
            capture_output=True,
            text=True,
            timeout=30,
        )

        hops = []
        for line in result.stdout.split("\n"):
            if line.strip() and line[0].isdigit():
                parts = line.split()
                if len(parts) >= 2:
                    hops.append({
                        "hop": int(parts[0]),
                        "ip": parts[1] if parts[1] != "*" else "Request timed out",
                    })

        return {"host": req.host, "hops": hops}
    except subprocess.TimeoutExpired:
        return {"host": req.host, "hops": [], "error": "Timeout"}
    except Exception as e:
        logger.error("Traceroute error: %s", e)
        return {"host": req.host, "hops": [], "error": str(e)}
```

- [ ] **Step 2: Register router**

```python
# Add to main.py
from .routers import system, network, websocket, ping, speedtest, settings, traceroute

app.include_router(traceroute.router)
```

- [ ] **Step 3: Test endpoint**

```bash
curl -X POST http://localhost:8000/api/traceroute -H "Content-Type: application/json" -d '{"host": "google.com"}'
```

- [ ] **Step 4: Commit**

```bash
git add backend/
git commit -m "feat: traceroute service"
```

---

## Task 9: Backend - Tests

**Files:**
- Create: `backend/tests/conftest.py`
- Create: `backend/tests/test_system.py`
- Create: `backend/tests/test_network.py`
- Create: `backend/tests/test_ping.py`

**Interfaces:**
- Produces: pytest test suite

- [ ] **Step 1: Create conftest**

```python
# backend/tests/conftest.py
import pytest
from fastapi.testclient import TestClient
from app.main import app

client = TestClient(app)
```

- [ ] **Step 2: Create system tests**

```python
# backend/tests/test_system.py
from .conftest import client

def test_system_endpoint():
    response = client.get("/api/system")
    assert response.status_code == 200
    data = response.json()
    assert "hostname" in data
    assert "cpu_percent" in data
    assert "ram_percent" in data
    assert 0 <= data["cpu_percent"] <= 100
    assert 0 <= data["ram_percent"] <= 100

def test_health_endpoint():
    response = client.get("/api/health")
    assert response.status_code == 200
    assert response.json() == {"status": "ok"}
```

- [ ] **Step 3: Create network tests**

```python
# backend/tests/test_network.py
from .conftest import client

def test_network_endpoint():
    response = client.get("/api/network")
    assert response.status_code == 200
    data = response.json()
    assert "ipv4" in data
    assert "mac_address" in data
    assert "public_ip" in data
```

- [ ] **Step 4: Create ping tests**

```python
# backend/tests/test_ping.py
from .conftest import client

def test_ping_endpoint():
    response = client.get("/api/ping?host=google.com")
    assert response.status_code == 200
    data = response.json()
    assert "host" in data
    assert "status" in data
    assert data["status"] in ["online", "timeout", "error"]
```

- [ ] **Step 5: Run tests**

```bash
cd backend
pip install pytest
pytest tests/ -v
```

- [ ] **Step 6: Commit**

```bash
git add backend/tests/
git commit -m "feat: backend tests with pytest"
```

---

## Task 10: Frontend - Types & Hooks

**Files:**
- Create: `frontend/src/types/index.ts`
- Create: `frontend/src/hooks/useWebSocket.ts`
- Create: `frontend/src/hooks/useSettings.ts`
- Create: `frontend/src/hooks/useChartData.ts`
- Create: `frontend/src/services/api.ts`

**Interfaces:**
- Consumes: Backend WebSocket and REST API
- Produces: TypeScript types and React hooks

- [ ] **Step 1: Create TypeScript types**

```typescript
// frontend/src/types/index.ts
export interface SystemData {
  cpu_percent: number
  ram_percent: number
  disk_percent: number
}

export interface TrafficData {
  upload_mbps: number
  download_mbps: number
  packets_sent: number
  packets_recv: number
}

export interface PingData {
  host: string
  latency_ms: number | null
  status: 'online' | 'timeout' | 'error'
}

export interface Alert {
  type: string
  message: string
  severity: 'warning' | 'error'
}

export interface LiveUpdate {
  type: string
  data: {
    system: SystemData
    traffic: TrafficData
    ping: PingData
    alerts: Alert[]
  }
  timestamp: number
}

export interface Settings {
  refresh_rate: number
  theme: 'dark' | 'light'
  ping_target: string
  notifications_enabled: boolean
}
```

- [ ] **Step 2: Create useWebSocket hook (single connection)**

```typescript
// frontend/src/hooks/useWebSocket.ts
import { useState, useEffect, useRef } from 'react'
import { LiveUpdate } from '@/types'

export function useWebSocket(host?: string) {
  const [data, setData] = useState<LiveUpdate | null>(null)
  const [connected, setConnected] = useState(false)
  const ws = useRef<WebSocket | null>(null)

  useEffect(() => {
    const wsUrl = host
      ? `${import.meta.env.VITE_WS_URL}/ws/live?host=${host}`
      : `${import.meta.env.VITE_WS_URL}/ws/live`

    const connect = () => {
      ws.current = new WebSocket(wsUrl)

      ws.current.onopen = () => setConnected(true)
      ws.current.onclose = () => {
        setConnected(false)
        setTimeout(connect, 3000)
      }
      ws.current.onmessage = (event) => {
        const json = JSON.parse(event.data)
        setData(json)
      }
    }

    connect()

    return () => {
      if (ws.current) ws.current.close()
    }
  }, [host])

  return { data, connected }
}
```

- [ ] **Step 3: Create useSettings hook**

```typescript
// frontend/src/hooks/useSettings.ts
import { useState, useEffect } from 'react'
import { Settings } from '@/types'

export function useSettings() {
  const [settings, setSettings] = useState<Settings>({
    refresh_rate: 1,
    theme: 'dark',
    ping_target: 'google.com',
    notifications_enabled: true,
  })

  const fetchSettings = async () => {
    const res = await fetch(`${import.meta.env.VITE_API_URL}/api/settings`)
    const data = await res.json()
    setSettings(data)
  }

  const updateSettings = async (updates: Partial<Settings>) => {
    const res = await fetch(`${import.meta.env.VITE_API_URL}/api/settings`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(updates),
    })
    const data = await res.json()
    setSettings(data)
  }

  useEffect(() => {
    fetchSettings()
  }, [])

  return { settings, updateSettings }
}
```

- [ ] **Step 4: Create useChartData hook**

```typescript
// frontend/src/hooks/useChartData.ts
import { useState } from 'react'

interface ChartPoint {
  time: string
  value: number
}

export function useChartData(maxPoints = 30) {
  const [history, setHistory] = useState<ChartPoint[]>([])

  const addPoint = (value: number) => {
    setHistory((prev) => {
      const newPoint: ChartPoint = {
        time: new Date().toLocaleTimeString(),
        value,
      }
      return [...prev, newPoint].slice(-maxPoints)
    })
  }

  return { history, addPoint }
}
```

- [ ] **Step 5: Create API service**

```typescript
// frontend/src/services/api.ts
const API_URL = import.meta.env.VITE_API_URL

export async function fetchSystem() {
  const res = await fetch(`${API_URL}/api/system`)
  return res.json()
}

export async function fetchNetwork() {
  const res = await fetch(`${API_URL}/api/network`)
  return res.json()
}

export async function pingHost(host: string) {
  const res = await fetch(`${API_URL}/api/ping?host=${host}`)
  return res.json()
}

export async function startSpeedtest() {
  const res = await fetch(`${API_URL}/api/speedtest`, { method: 'POST' })
  return res.json()
}

export async function getSpeedtestResult(jobId: string) {
  const res = await fetch(`${API_URL}/api/speedtest/${jobId}`)
  return res.json()
}

export async function traceroute(host: string) {
  const res = await fetch(`${API_URL}/api/traceroute`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ host }),
  })
  return res.json()
}
```

- [ ] **Step 6: Commit**

```bash
git add frontend/src/types frontend/src/hooks frontend/src/services
git commit -m "feat: TypeScript types and React hooks"
```

---

## Task 11: Frontend - Layout & Routing

**Files:**
- Create: `frontend/src/components/layout/Sidebar.tsx`
- Create: `frontend/src/pages/DashboardPage.tsx`
- Create: `frontend/src/pages/SettingsPage.tsx`
- Create: `frontend/src/pages/TraceroutePage.tsx`
- Modify: `frontend/src/App.tsx`

**Interfaces:**
- Consumes: react-router-dom
- Produces: Page routing and layout

- [ ] **Step 1: Create Sidebar**

```tsx
// frontend/src/components/layout/Sidebar.tsx
import { NavLink } from 'react-router-dom'
import { LayoutDashboard, Wifi, Gauge, Globe, History, Settings } from 'lucide-react'

const navItems = [
  { icon: LayoutDashboard, label: 'Dashboard', path: '/' },
  { icon: Wifi, label: 'Network', path: '/network' },
  { icon: Gauge, label: 'Speed Test', path: '/speedtest' },
  { icon: Globe, label: 'Traceroute', path: '/traceroute' },
  { icon: History, label: 'History', path: '/history' },
  { icon: Settings, label: 'Settings', path: '/settings' },
]

export default function Sidebar() {
  return (
    <aside className="w-64 bg-slate-900 border-r border-slate-800 p-4">
      <div className="flex items-center gap-2 mb-8">
        <div className="w-8 h-8 bg-blue-500 rounded-lg flex items-center justify-center">
          <Wifi className="w-5 h-5 text-white" />
        </div>
        <span className="text-xl font-bold">NetPulse</span>
      </div>

      <nav className="space-y-2">
        {navItems.map(({ icon: Icon, label, path }) => (
          <NavLink
            key={path}
            to={path}
            className={({ isActive }) =>
              `flex items-center gap-3 px-4 py-2 rounded-lg transition-colors ${
                isActive ? 'bg-blue-500/20 text-blue-400' : 'text-slate-400 hover:bg-slate-800'
              }`
            }
          >
            <Icon className="w-5 h-5" />
            {label}
          </NavLink>
        ))}
      </nav>
    </aside>
  )
}
```

- [ ] **Step 2: Create DashboardPage**

```tsx
// frontend/src/pages/DashboardPage.tsx
import { useWebSocket } from '@/hooks/useWebSocket'
import CpuCard from '@/components/cards/CpuCard'
import RamCard from '@/components/cards/RamCard'
import DiskCard from '@/components/cards/DiskCard'
import NetworkCard from '@/components/cards/NetworkCard'
import PingCard from '@/components/cards/PingCard'
import CpuChart from '@/components/charts/CpuChart'
import TrafficChart from '@/components/charts/TrafficChart'
import PingChart from '@/components/charts/PingChart'

export default function DashboardPage() {
  const { data } = useWebSocket()

  return (
    <div className="p-4">
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 mb-4">
        <CpuCard data={data?.data.system} />
        <RamCard data={data?.data.system} />
        <DiskCard data={data?.data.system} />
        <NetworkCard data={data?.data.traffic} />
        <PingCard data={data?.data.ping} />
      </div>
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-4">
        <CpuChart data={data?.data.system} />
        <TrafficChart data={data?.data.traffic} />
        <PingChart data={data?.data.ping} />
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Create SettingsPage**

```tsx
// frontend/src/pages/SettingsPage.tsx
import { useSettings } from '@/hooks/useSettings'

export default function SettingsPage() {
  const { settings, updateSettings } = useSettings()

  return (
    <div className="p-4 max-w-2xl">
      <h2 className="text-2xl font-bold mb-6">Settings</h2>
      <div className="space-y-6">
        <div className="bg-slate-900 rounded-xl p-4 border border-slate-800">
          <label className="text-sm text-slate-400 mb-2 block">Refresh Rate</label>
          <select
            value={settings.refresh_rate}
            onChange={(e) => updateSettings({ refresh_rate: Number(e.target.value) })}
            className="bg-slate-800 text-white rounded-lg px-4 py-2 w-full"
          >
            <option value={1}>1 second</option>
            <option value={2}>2 seconds</option>
            <option value={5}>5 seconds</option>
          </select>
        </div>
        <div className="bg-slate-900 rounded-xl p-4 border border-slate-800">
          <label className="text-sm text-slate-400 mb-2 block">Theme</label>
          <select
            value={settings.theme}
            onChange={(e) => updateSettings({ theme: e.target.value as 'dark' | 'light' })}
            className="bg-slate-800 text-white rounded-lg px-4 py-2 w-full"
          >
            <option value="dark">Dark</option>
            <option value="light">Light</option>
          </select>
        </div>
        <div className="bg-slate-900 rounded-xl p-4 border border-slate-800">
          <label className="text-sm text-slate-400 mb-2 block">Default Ping Target</label>
          <select
            value={settings.ping_target}
            onChange={(e) => updateSettings({ ping_target: e.target.value })}
            className="bg-slate-800 text-white rounded-lg px-4 py-2 w-full"
          >
            <option value="google.com">Google (8.8.8.8)</option>
            <option value="cloudflare.com">Cloudflare (1.1.1.1)</option>
          </select>
        </div>
        <div className="bg-slate-900 rounded-xl p-4 border border-slate-800">
          <label className="text-sm text-slate-400 mb-2 block">Notifications</label>
          <button
            onClick={() => updateSettings({ notifications_enabled: !settings.notifications_enabled })}
            className={`px-4 py-2 rounded-lg ${
              settings.notifications_enabled
                ? 'bg-green-500 text-white'
                : 'bg-slate-700 text-slate-300'
            }`}
          >
            {settings.notifications_enabled ? 'Enabled' : 'Disabled'}
          </button>
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Create TraceroutePage**

```tsx
// frontend/src/pages/TraceroutePage.tsx
import { useState } from 'react'
import { traceroute } from '@/services/api'

export default function TraceroutePage() {
  const [host, setHost] = useState('')
  const [hops, setHops] = useState<{ hop: number; ip: string }[]>([])
  const [loading, setLoading] = useState(false)

  const runTraceroute = async () => {
    setLoading(true)
    const data = await traceroute(host)
    setHops(data.hops || [])
    setLoading(false)
  }

  return (
    <div className="p-4 max-w-2xl">
      <h2 className="text-2xl font-bold mb-6">Traceroute</h2>
      <div className="flex gap-2 mb-6">
        <input
          value={host}
          onChange={(e) => setHost(e.target.value)}
          placeholder="Enter host (e.g., google.com)"
          className="flex-1 bg-slate-800 rounded-lg px-4 py-2"
        />
        <button
          onClick={runTraceroute}
          disabled={loading}
          className="bg-blue-500 hover:bg-blue-600 px-6 py-2 rounded-lg"
        >
          {loading ? 'Tracing...' : 'Trace'}
        </button>
      </div>
      <div className="space-y-2">
        {hops.map((hop, i) => (
          <div key={i} className="flex items-center gap-4 bg-slate-900 rounded-lg p-3">
            <span className="text-slate-500 w-8">{hop.hop}</span>
            <span className="flex-1">{hop.ip}</span>
          </div>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 5: Update App.tsx with React Router**

```tsx
// frontend/src/App.tsx
import { BrowserRouter, Routes, Route } from 'react-router-dom'
import Sidebar from '@/components/layout/Sidebar'
import DashboardPage from '@/pages/DashboardPage'
import SettingsPage from '@/pages/SettingsPage'
import TraceroutePage from '@/pages/TraceroutePage'

function App() {
  return (
    <BrowserRouter>
      <div className="flex h-screen bg-slate-950 text-white">
        <Sidebar />
        <main className="flex-1 overflow-auto">
          <Routes>
            <Route path="/" element={<DashboardPage />} />
            <Route path="/settings" element={<SettingsPage />} />
            <Route path="/traceroute" element={<TraceroutePage />} />
          </Routes>
        </main>
      </div>
    </BrowserRouter>
  )
}

export default App
```

- [ ] **Step 6: Commit**

```bash
git add frontend/
git commit -m "feat: React Router + page components"
```

---

## Task 12: Frontend - Dashboard Cards

**Files:**
- Create: `frontend/src/components/cards/CpuCard.tsx`
- Create: `frontend/src/components/cards/RamCard.tsx`
- Create: `frontend/src/components/cards/DiskCard.tsx`
- Create: `frontend/src/components/cards/NetworkCard.tsx`
- Create: `frontend/src/components/cards/PingCard.tsx`

**Interfaces:**
- Consumes: TypeScript types from Task 10
- Produces: Dashboard card components

- [ ] **Step 1: Create CpuCard**

```tsx
// frontend/src/components/cards/CpuCard.tsx
import { Cpu } from 'lucide-react'
import { Progress } from '@/components/ui/progress'
import { SystemData } from '@/types'

interface Props {
  data?: SystemData
}

export default function CpuCard({ data }: Props) {
  const percent = data?.cpu_percent || 0

  return (
    <div className="bg-slate-900 rounded-xl p-4 border border-slate-800">
      <div className="flex items-center gap-2 mb-3">
        <Cpu className="w-5 h-5 text-blue-400" />
        <span className="text-sm text-slate-400">CPU Usage</span>
      </div>
      <div className="text-3xl font-bold mb-2">{percent}%</div>
      <Progress value={percent} className="h-2" />
    </div>
  )
}
```

- [ ] **Step 2: Create RamCard**

```tsx
// frontend/src/components/cards/RamCard.tsx
import { MemoryStick } from 'lucide-react'
import { Progress } from '@/components/ui/progress'
import { SystemData } from '@/types'

interface Props {
  data?: SystemData
}

export default function RamCard({ data }: Props) {
  const percent = data?.ram_percent || 0

  return (
    <div className="bg-slate-900 rounded-xl p-4 border border-slate-800">
      <div className="flex items-center gap-2 mb-3">
        <MemoryStick className="w-5 h-5 text-green-400" />
        <span className="text-sm text-slate-400">RAM</span>
      </div>
      <div className="text-3xl font-bold mb-2">{percent}%</div>
      <Progress value={percent} className="h-2" />
    </div>
  )
}
```

- [ ] **Step 3: Create DiskCard**

```tsx
// frontend/src/components/cards/DiskCard.tsx
import { HardDrive } from 'lucide-react'
import { Progress } from '@/components/ui/progress'
import { SystemData } from '@/types'

interface Props {
  data?: SystemData
}

export default function DiskCard({ data }: Props) {
  const percent = data?.disk_percent || 0

  return (
    <div className="bg-slate-900 rounded-xl p-4 border border-slate-800">
      <div className="flex items-center gap-2 mb-3">
        <HardDrive className="w-5 h-5 text-purple-400" />
        <span className="text-sm text-slate-400">Disk</span>
      </div>
      <div className="text-3xl font-bold mb-2">{percent}%</div>
      <Progress value={percent} className="h-2" />
    </div>
  )
}
```

- [ ] **Step 4: Create NetworkCard**

```tsx
// frontend/src/components/cards/NetworkCard.tsx
import { ArrowUp, ArrowDown } from 'lucide-react'
import { TrafficData } from '@/types'

interface Props {
  data?: TrafficData
}

export default function NetworkCard({ data }: Props) {
  return (
    <div className="bg-slate-900 rounded-xl p-4 border border-slate-800">
      <div className="flex items-center gap-2 mb-3">
        <ArrowUp className="w-5 h-5 text-emerald-400" />
        <span className="text-sm text-slate-400">Network Traffic</span>
      </div>
      <div className="space-y-2">
        <div className="flex justify-between">
          <span className="text-slate-400">Upload</span>
          <span className="font-bold text-emerald-400">{data?.upload_mbps || 0} MB/s</span>
        </div>
        <div className="flex justify-between">
          <span className="text-slate-400">Download</span>
          <span className="font-bold text-blue-400">{data?.download_mbps || 0} MB/s</span>
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 5: Create PingCard**

```tsx
// frontend/src/components/cards/PingCard.tsx
import { Wifi, WifiOff } from 'lucide-react'
import { PingData } from '@/types'

interface Props {
  data?: PingData
}

export default function PingCard({ data }: Props) {
  const latency = data?.latency_ms
  const status = data?.status

  return (
    <div className="bg-slate-900 rounded-xl p-4 border border-slate-800">
      <div className="flex items-center gap-2 mb-3">
        {status === 'online' ? (
          <Wifi className="w-5 h-5 text-green-400" />
        ) : (
          <WifiOff className="w-5 h-5 text-red-400" />
        )}
        <span className="text-sm text-slate-400">Ping</span>
      </div>
      <div className="text-3xl font-bold mb-1">
        {latency !== null && latency !== undefined ? `${latency} ms` : '---'}
      </div>
      <div className={`text-sm ${status === 'online' ? 'text-green-400' : 'text-red-400'}`}>
        {status === 'online' ? '● Online' : '● Offline'}
      </div>
    </div>
  )
}
```

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/cards/
git commit -m "feat: dashboard cards with TypeScript"
```

---

## Task 13: Frontend - Charts

**Files:**
- Create: `frontend/src/components/charts/CpuChart.tsx`
- Create: `frontend/src/components/charts/TrafficChart.tsx`
- Create: `frontend/src/components/charts/PingChart.tsx`

**Interfaces:**
- Consumes: useChartData hook
- Produces: Real-time line charts

- [ ] **Step 1: Create CpuChart**

```tsx
// frontend/src/components/charts/CpuChart.tsx
import { useEffect } from 'react'
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from 'recharts'
import { useChartData } from '@/hooks/useChartData'
import { SystemData } from '@/types'

interface Props {
  data?: SystemData
}

export default function CpuChart({ data }: Props) {
  const { history, addPoint } = useChartData(30)

  useEffect(() => {
    if (data?.cpu_percent !== undefined) {
      addPoint(data.cpu_percent)
    }
  }, [data])

  return (
    <div className="bg-slate-900 rounded-xl p-4 border border-slate-800">
      <h3 className="text-sm text-slate-400 mb-4">CPU Usage Over Time</h3>
      <ResponsiveContainer width="100%" height={200}>
        <LineChart data={history}>
          <CartesianGrid strokeDasharray="3 3" stroke="#334155" />
          <XAxis dataKey="time" tick={false} />
          <YAxis domain={[0, 100]} stroke="#64748b" />
          <Tooltip
            contentStyle={{ backgroundColor: '#1e293b', border: '1px solid #334155' }}
            labelStyle={{ color: '#94a3b8' }}
          />
          <Line type="monotone" dataKey="value" stroke="#3b82f6" dot={false} />
        </LineChart>
      </ResponsiveContainer>
    </div>
  )
}
```

- [ ] **Step 2: Create TrafficChart**

```tsx
// frontend/src/components/charts/TrafficChart.tsx
import { useEffect, useState } from 'react'
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Legend } from 'recharts'
import { TrafficData } from '@/types'

interface Props {
  data?: TrafficData
}

interface TrafficPoint {
  time: string
  upload: number
  download: number
}

export default function TrafficChart({ data }: Props) {
  const [history, setHistory] = useState<TrafficPoint[]>([])

  useEffect(() => {
    if (data) {
      setHistory((prev) => {
        const newPoint: TrafficPoint = {
          time: new Date().toLocaleTimeString(),
          upload: data.upload_mbps,
          download: data.download_mbps,
        }
        return [...prev, newPoint].slice(-30)
      })
    }
  }, [data])

  return (
    <div className="bg-slate-900 rounded-xl p-4 border border-slate-800">
      <h3 className="text-sm text-slate-400 mb-4">Network Traffic</h3>
      <ResponsiveContainer width="100%" height={200}>
        <LineChart data={history}>
          <CartesianGrid strokeDasharray="3 3" stroke="#334155" />
          <XAxis dataKey="time" tick={false} />
          <YAxis stroke="#64748b" />
          <Tooltip
            contentStyle={{ backgroundColor: '#1e293b', border: '1px solid #334155' }}
            labelStyle={{ color: '#94a3b8' }}
          />
          <Legend />
          <Line type="monotone" dataKey="upload" stroke="#22c55e" dot={false} name="Upload" />
          <Line type="monotone" dataKey="download" stroke="#3b82f6" dot={false} name="Download" />
        </LineChart>
      </ResponsiveContainer>
    </div>
  )
}
```

- [ ] **Step 3: Create PingChart**

```tsx
// frontend/src/components/charts/PingChart.tsx
import { useEffect, useState } from 'react'
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from 'recharts'
import { PingData } from '@/types'

interface Props {
  data?: PingData
}

interface PingPoint {
  time: string
  latency: number
}

export default function PingChart({ data }: Props) {
  const [history, setHistory] = useState<PingPoint[]>([])

  useEffect(() => {
    if (data?.latency_ms !== null && data?.latency_ms !== undefined) {
      setHistory((prev) => {
        const newPoint: PingPoint = {
          time: new Date().toLocaleTimeString(),
          latency: data.latency_ms!,
        }
        return [...prev, newPoint].slice(-30)
      })
    }
  }, [data])

  return (
    <div className="bg-slate-900 rounded-xl p-4 border border-slate-800">
      <h3 className="text-sm text-slate-400 mb-4">Ping Latency</h3>
      <ResponsiveContainer width="100%" height={200}>
        <LineChart data={history}>
          <CartesianGrid strokeDasharray="3 3" stroke="#334155" />
          <XAxis dataKey="time" tick={false} />
          <YAxis stroke="#64748b" />
          <Tooltip
            contentStyle={{ backgroundColor: '#1e293b', border: '1px solid #334155' }}
            labelStyle={{ color: '#94a3b8' }}
          />
          <Line type="monotone" dataKey="latency" stroke="#a855f7" dot={false} />
        </LineChart>
      </ResponsiveContainer>
    </div>
  )
}
```

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/charts/
git commit -m "feat: live charts with TypeScript"
```

---

## Task 14: Frontend - Notifications (from Backend)

**Files:**
- Create: `frontend/src/components/notifications/Toast.tsx`
- Modify: `frontend/src/pages/DashboardPage.tsx`

**Interfaces:**
- Consumes: alerts from WebSocket (backend detection)
- Produces: Toast notifications

- [ ] **Step 1: Create Toast component**

```tsx
// frontend/src/components/notifications/Toast.tsx
import { useState, useEffect } from 'react'
import { X, AlertTriangle, WifiOff } from 'lucide-react'
import { Alert } from '@/types'

interface Props {
  alerts: Alert[]
}

export default function Toast({ alerts }: Props) {
  const [visible, setVisible] = useState<Alert[]>([])

  useEffect(() => {
    if (alerts.length > 0) {
      setVisible((prev) => [...prev, ...alerts].slice(-3))

      const timer = setTimeout(() => {
        setVisible((prev) => prev.slice(alerts.length))
      }, 5000)

      return () => clearTimeout(timer)
    }
  }, [alerts])

  const dismiss = (index: number) => {
    setVisible((prev) => prev.filter((_, i) => i !== index))
  }

  if (visible.length === 0) return null

  return (
    <div className="fixed bottom-4 right-4 space-y-2 z-50">
      {visible.map((alert, i) => (
        <div
          key={`${alert.type}-${i}`}
          className={`flex items-center gap-3 px-4 py-3 rounded-lg shadow-lg ${
            alert.severity === 'warning'
              ? 'bg-yellow-500/20 border border-yellow-500/50'
              : 'bg-red-500/20 border border-red-500/50'
          }`}
        >
          {alert.severity === 'warning' ? (
            <AlertTriangle className="w-5 h-5 text-yellow-400" />
          ) : (
            <WifiOff className="w-5 h-5 text-red-400" />
          )}
          <span className="text-sm">{alert.message}</span>
          <button onClick={() => dismiss(i)} className="ml-2 text-slate-400 hover:text-white">
            <X className="w-4 h-4" />
          </button>
        </div>
      ))}
    </div>
  )
}
```

- [ ] **Step 2: Update DashboardPage to use Toast**

```tsx
// frontend/src/pages/DashboardPage.tsx
import { useWebSocket } from '@/hooks/useWebSocket'
import CpuCard from '@/components/cards/CpuCard'
import RamCard from '@/components/cards/RamCard'
import DiskCard from '@/components/cards/DiskCard'
import NetworkCard from '@/components/cards/NetworkCard'
import PingCard from '@/components/cards/PingCard'
import CpuChart from '@/components/charts/CpuChart'
import TrafficChart from '@/components/charts/TrafficChart'
import PingChart from '@/components/charts/PingChart'
import Toast from '@/components/notifications/Toast'

export default function DashboardPage() {
  const { data } = useWebSocket()

  return (
    <div className="p-4">
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 mb-4">
        <CpuCard data={data?.data.system} />
        <RamCard data={data?.data.system} />
        <DiskCard data={data?.data.system} />
        <NetworkCard data={data?.data.traffic} />
        <PingCard data={data?.data.ping} />
      </div>
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-4">
        <CpuChart data={data?.data.system} />
        <TrafficChart data={data?.data.traffic} />
        <PingChart data={data?.data.ping} />
      </div>
      <Toast alerts={data?.data.alerts || []} />
    </div>
  )
}
```

- [ ] **Step 3: Commit**

```bash
git add frontend/
git commit -m "feat: toast notifications from backend alerts"
```

---

## Task 15: Frontend - Tests

**Files:**
- Create: `frontend/src/components/__tests__/CpuCard.test.tsx`
- Create: `frontend/src/components/__tests__/Sidebar.test.tsx`
- Create: `frontend/src/components/__tests__/Toast.test.tsx`

**Interfaces:**
- Produces: Vitest test suite

- [ ] **Step 1: Configure Vitest**

```typescript
// frontend/vitest.config.ts
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: './src/test/setup.ts',
  },
})
```

```typescript
// frontend/src/test/setup.ts
import '@testing-library/jest-dom'
```

- [ ] **Step 2: Create CpuCard test**

```tsx
// frontend/src/components/__tests__/CpuCard.test.tsx
import { render, screen } from '@testing-library/react'
import CpuCard from '../cards/CpuCard'

describe('CpuCard', () => {
  it('renders CPU percentage', () => {
    render(<CpuCard data={{ cpu_percent: 45, ram_percent: 0, disk_percent: 0 }} />)
    expect(screen.getByText('45%')).toBeInTheDocument()
  })

  it('renders 0% when no data', () => {
    render(<CpuCard />)
    expect(screen.getByText('0%')).toBeInTheDocument()
  })
})
```

- [ ] **Step 3: Create Sidebar test**

```tsx
// frontend/src/components/__tests__/Sidebar.test.tsx
import { render, screen } from '@testing-library/react'
import { BrowserRouter } from 'react-router-dom'
import Sidebar from '../layout/Sidebar'

describe('Sidebar', () => {
  it('renders navigation links', () => {
    render(
      <BrowserRouter>
        <Sidebar />
      </BrowserRouter>
    )
    expect(screen.getByText('Dashboard')).toBeInTheDocument()
    expect(screen.getByText('Settings')).toBeInTheDocument()
    expect(screen.getByText('Traceroute')).toBeInTheDocument()
  })
})
```

- [ ] **Step 4: Create Toast test**

```tsx
// frontend/src/components/__tests__/Toast.test.tsx
import { render, screen } from '@testing-library/react'
import Toast from '../notifications/Toast'

describe('Toast', () => {
  it('renders alert messages', () => {
    const alerts = [
      { type: 'cpu_high', message: 'High CPU: 95%', severity: 'warning' as const },
    ]
    render(<Toast alerts={alerts} />)
    expect(screen.getByText('High CPU: 95%')).toBeInTheDocument()
  })

  it('renders nothing when no alerts', () => {
    const { container } = render(<Toast alerts={[]} />)
    expect(container.firstChild).toBeNull()
  })
})
```

- [ ] **Step 5: Run tests**

```bash
cd frontend
pnpm test
```

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/__tests__
git commit -m "feat: frontend tests with Vitest + React Testing Library"
```

---

## Task 16: Formatters & Final Config

**Files:**
- Create: `frontend/.prettierrc`
- Create: `frontend/.eslintrc.cjs`
- Create: `backend/pyproject.toml` (ruff + black)

**Interfaces:**
- Produces: Code formatting config

- [ ] **Step 1: Create prettier config**

```json
// frontend/.prettierrc
{
  "semi": false,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100
}
```

- [ ] **Step 2: Create eslint config**

```javascript
// frontend/.eslintrc.cjs
module.exports = {
  root: true,
  env: { browser: true, es2020: true },
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react-hooks/recommended',
    'prettier',
  ],
  ignorePatterns: ['dist'],
}
```

- [ ] **Step 3: Create README.md**

```markdown
# NetPulse - Network Monitoring Dashboard

A real-time network monitoring dashboard built with React, TypeScript, and FastAPI.

## Features

- Real-time CPU, RAM, Disk monitoring via WebSocket
- Live network traffic charts
- Ping monitor with alerts
- Speed test (background job)
- Traceroute visualization
- Dark/Light theme
- Backend notification system

## Tech Stack

- **Frontend:** React, TypeScript, Tailwind CSS, Recharts, Lucide Icons
- **Backend:** FastAPI, WebSocket, psutil, ping3
- **Database:** JSON (v1) → SQLite (v2)
- **Package Manager:** pnpm

## Quick Start

### Backend

```bash
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env
uvicorn app.main:app --reload
```

### Frontend

```bash
cd frontend
pnpm install
cp .env.example .env
pnpm dev
```

Open http://localhost:5173

## API Docs

Visit http://localhost:8000/docs for Swagger UI

## Testing

```bash
# Backend
cd backend && pytest

# Frontend
cd frontend && pnpm test
```

## Formatting

```bash
# Backend
cd backend && ruff format . && black .

# Frontend
cd frontend && pnpm format
```
```

- [ ] **Step 4: Final test**

Test all features:
1. Dashboard loads with live data
2. All cards update in real-time
3. Charts show history
4. Settings page works
5. Notifications appear on high CPU (backend detected)
6. Traceroute works

- [ ] **Step 5: Final commit**

```bash
git add -A
git commit -m "feat: NetPulse v1 complete - TypeScript, WebSocket, testing, formatters"
```

---

## Summary

| Task | Description | Key Changes |
|------|-------------|-------------|
| 1 | Project scaffolding | pnpm, TypeScript, .env |
| 2 | System info service | Logging |
| 3 | Network info service | Logging |
| 4 | Unified WebSocket | Single `/ws/live` connection |
| 5 | Ping service | Logging |
| 6 | Speedtest | Background job pattern |
| 7 | Settings | JSON file, not SQLite |
| 8 | Traceroute | Logging |
| 9 | Backend tests | pytest |
| 10 | Types & hooks | TypeScript, useWebSocket |
| 11 | Layout & routing | react-router-dom |
| 12 | Dashboard cards | TypeScript props |
| 13 | Charts | TypeScript |
| 14 | Notifications | Backend detection → frontend Toast |
| 15 | Frontend tests | Vitest + React Testing Library |
| 16 | Formatters & config | ruff, black, eslint, prettier |

**Estimated time:** 5-7 hours

---

*Plan version 2.0 — Revised with all 12 corrections.*
